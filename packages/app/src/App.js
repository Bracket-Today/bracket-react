import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  Text,
  View,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>Bracket</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default App;
